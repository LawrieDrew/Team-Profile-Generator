const Employee = require("./Employee");

    class Manager extends Employee {
        constructor(name, id, email, officeNumber){
        super(name, id, email);
        this.officeNumber = officeNumber;
    }

    getOfficeNumber() {
        return this.officeNumber;
    }

    getRole() {
        return "Manager";
    }
}
module.exports = Manager;

//write html file and erase all cards for team members
copy chunk, put in stirng SVGPathSegLinetoVerticalAbs, thasts where you put functions
then that function 
inside of indexedDB, 
fs write file 

const generate engineer 